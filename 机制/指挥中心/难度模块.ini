[核心]
禁止加载:true
[隐藏行动_难度显示]
自动触发:if 存活时间(指定时间后=1) and not 自身有标签(包含标签="难度判断完毕")
需要条件:if 自身属于AI控制
设置单位内存:难度=select(自身资源.AI难度>=5, '终焉', select(自身资源.AI难度>=4, '绝境', select(自身资源.AI难度>=3.5, '不可能', select(自身资源.AI难度>=3, '有可能', select(自身资源.AI难度>=2.5, '非常困难', select(自身资源.AI难度>=2, '困难', select(自身资源.AI难度>=1.5, '一般', select(自身资源.AI难度>=1, '普通', '简单')))))))), 通告=select(自身资源.AI难度>=5, '英勇的凡灵，为你胆敢僭越天神步辇的行为予以赞歌', select(自身资源.AI难度>=4, '小友，还请留步！', '道友请留步！'))
也执行动作:通告
临时标签添加:难度判断完毕
[隐藏行动_通告]
需要条件:if 自身资源.AI难度>=3 and numberOfUnitsInAllTeams(需标签="通告", 少于=1)
发送消息至所有:%{内存.通告}
临时标签添加:通告